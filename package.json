{
  "name": "chrome-pcs-tooltips",
  "version": "0.2",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/pcs-tooltip.js --outfile=dist/pcs-tooltip.js --bundle --minify",
    "watch": "esbuild src/pcs-tooltip.js --outfile=dist/pcs-tooltip.js --bundle --watch",
    "lint": "eslint src/*.js"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.11.9",
    "eslint": "^7.29.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1"
  }
}
