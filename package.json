{
  "name": "chrome-pcs-tooltips",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/pcs-tooltip.js --outfile=pcs-tooltip.js --bundle --minify",
    "watch": "esbuild src/pcs-tooltip.js --outfile=pcs-tooltip.js --bundle --watch",
    "lint": "eslint src/*.js"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.9.2",
    "eslint": "^7.22.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "watchify": "^4.0.0"
  }
}
